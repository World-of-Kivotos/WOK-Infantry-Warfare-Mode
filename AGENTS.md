# WOK 产品线永久命名规则

以下规则适用于本项目及其所有后续开发、文档、构建产物和讨论：

- `WOK` 与 `WOK步战` 是两个不同概念，不得混称。
- `WOK` 指主项目/本体产品线。
- `WOK步战` 指依附于相关生态的附加子模式及其可拆分附属 MOD，不等同于 WOK 本体。
- 已上传 Git、服务于 WOK 本体的护甲实现，正式称为 `WOK-本体护甲`。保留其现有仓库身份与 modId，避免破坏兼容性。
- 从本体拆分、能够单独安装的纯护甲 MOD，正式称为 `WOK步战附属-独立护甲`，建议 modId 为 `wok_infantry_armor`，JAR 前缀为 `wok_infantry_armor-`。
- `WOK步战附属-部位血量` 的 modId 为 `wok_body_health`。
- `WOK步战附属-创伤治疗` 的 modId 为 `wok_trauma`。
- `WOK步战附属-载具部位血量` 的 modId 为 `wok_vehicle_health`。
- `WOK步战附属-指挥官支援` 的 modId 为 `wok_commander_support`。
- `WOK步战附属-占点` 的 modId 为 `wok_capture_points`。
- `WOK步战核心` 的 modId 为 `wok_infantry`。
- 未特别说明时，部位防护、部位血量和创伤治疗等新联动只加入 `WOK步战附属-独立护甲`，不得误改或回灌到 `WOK-本体护甲`。
- 所有拆分 MOD 必须保持可独立安装；跨 MOD 功能通过可选依赖或软联动实现。
- `D:\WOK步战测试\1.20.1-Forge_47.4.22\mods` 是 WOK步战附属专用测试目录；步战核心、创伤、部位血量、独立护甲、指挥官支援和占点只在这里测试。
- `D:\WOK测试\versions\1.20.1-Forge_47.4.20\mods` 是 WOK 本体专用测试目录；不得把这里的本体 JAR 当成步战附属或独立护甲。
- `WOK步战附属-独立护甲` 的 Git 权威源码目录是本仓库的 `wok_infantry_armor/`；正式 modId 为 `wok_infantry_armor`，Java 包根为 `com.wok.infantryarmor`。`D:\Wok\standalone\wok-infantry-armor` 仅保留为迁移前本地副本，不得与 Git 版本分别开发。
- 每个 MOD 独立维护语义化版本号，不得因其他模块发布而被动升级。
- 每次新增功能、物品、载具、配装、UI 或其他游戏内容时，必须更新所属 MOD 的版本号，不得因“尚未正式发布”而继续沿用旧号；测试包也必须使用可区分的新版本号。
- 已交付版本之后，影响游戏行为、资源、配置或兼容性的修复和调整也必须使用新版本；禁止把不同交付内容的 JAR 标成同一个版本。尚未交付的同一轮实现、修正和验收可共用本轮新版本。
- 纯文档修正、无交付内容变化的提交和内部重构不单独提升版本。版本递增及预发行规则统一遵循 `docs/VERSIONING.md`。
- 版本提升必须同时更新根目录 `CHANGELOG.md`，并完成对应模块构建、独立安装检查和游戏内验证。
- 发布说明必须明确列出新增、修改、修复、兼容性、配置/存档影响和测试结果；没有内容的栏目写“无”，不得省略。

# WOK步战运输补给永久规则

- 车辆运输补给的权威清单固定维护在 `wok_infantry/docs/SUPPLY_TRANSPORT_CAPACITY_LIST.md`；今后每新增、删除或调整一种车辆，都必须同步更新该表、服务端默认配置、相关测试与验收说明，不得只在对话或代码中记录。
- FMTV 的实体注册 ID 为 `superbwarfare:truck`，每辆满载时携带 3 个大型补给。
- Ural 的实体注册 ID 为 `fcp:ural`，每辆满载时携带 3 个大型补给。
- 无装甲无武装 HMMWV 的实体注册 ID 为 `fcp:hmmwv_unarmored_unarmed`，每辆满载时携带 2 个中型补给。
- 装甲无武装 HMMWV 的实体注册 ID 为 `fcp:hmmwv_armored_unarmed`，每辆满载时携带 1 个中型补给。
- 清单中的“补给规格”和“容量”共同构成车辆运输配置；大型与中型不得互换。容量表示新建立运输数据的满载数量；车辆当前剩余量必须继续持久化，重启或提高配置容量不得自动补满旧车。

# WOK步战 UI 永久风格规则

以下规则适用于 `WOK步战` 核心及其所有附属 MOD 的新 UI、旧 UI 重制和后续扩展；不自动套用到 `WOK` 本体产品线：

- 默认视觉语言固定为当前已经实现的“战术平板 / 实体战术拼板”风格。参考《Into the Radius 2》、WARDOGS 与《Squad》的信息组织和战术终端氛围，但不得复制其代码、素材、图标、商标或界面成品。
- 所有同类界面优先复用 `WOK步战核心` 的 `TacticalBoardChrome`、`TacticalBoardTheme`、`BattleUiButton` 及其后续统一组件，不得为单个页面另起一套互不兼容的皮肤。
- 使用深色设备外框、灰绿战术面板、细描边、铆钉或机械拼板层级；蓝色表示当前选择，橙色表示分区与可调控件，红色表示危险/敌对操作，绿色表示成功或确认状态，灰色表示禁用。
- 字体以清晰、低重影和高对比度为优先，不使用默认文字阴影堆叠；标题、按钮和状态文字必须在实际控件宽度内裁切或缩放，不得越界、重叠或遮挡。
- 所有界面必须响应式适配至少 `320×240` 与 `960×720` 两档逻辑分辨率；窄屏允许重排、分页或收起次要信息，但不得丢失核心操作。
- 按钮必须明确区分普通、悬停、选中、禁用、危险、成功和可调控制状态；同一状态在小队、兵种、配装、部署、地图、管理和医疗 UI 中保持一致。
- 战术图标使用透明背景、清晰轮廓和稳定的屏幕物理尺寸；地图缩放不得导致图标反向膨胀，用户尺寸旋钮与最低可读尺寸必须继续保留。
- 新 UI 完成前必须进行真实游戏客户端视觉验收；至少检查窄屏与大屏、文字溢出、控件重叠、按钮状态、字体清晰度和关键交互。
- 除非用户明确要求更换风格，今后所有 `WOK步战` UI 设计与实现均以本节规则为默认基线。

# WOK步战阵营与编制文档规则

- 编写或导入阵营、编制设计文档时，读取并遵循 `wok_infantry/docs/FORMATION_AUTHORING_RULES.md`；公示格式参考《步战模式公示表》的“阵营”表。
- 文档导入按明确操作、范围和稳定 ID 合并，保留未涉及的已有阵营、编制与配装；不得将空白、漏行或格式参考解释为删除、覆盖或启用授权。
- 区分组织分组与公开战斗阵营，核对资源 ID、数量、时间及名额作用域；未实现机制与未完成映射须明确记录，不得宣称已完成导入。
