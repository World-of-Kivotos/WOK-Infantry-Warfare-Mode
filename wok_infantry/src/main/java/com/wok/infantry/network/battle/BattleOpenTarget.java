package com.wok.infantry.network.battle;

/** The client UI that should be opened after applying a server-filtered snapshot. */
public enum BattleOpenTarget {
    NONE,
    SQUAD,
    DEPLOYMENT,
    MAP
}
